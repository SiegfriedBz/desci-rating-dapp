node_modules/
.next/
dist/
out/
*.tsbuildinfo
cache
broadcast
out-foundry
.env
.env.local
.dkg-data
*.log

# Agent skill installs (local; lockfile optional for team)
.agents/
.claude/
agent/
skills-lock.json

# Cursor: .cursor/rules/monorepo.mdc and web.mdc stay tracked (they describe
# this repo). These two are per-developer.
.cursor/mcp.json
.cursor/rules/git.mdc
