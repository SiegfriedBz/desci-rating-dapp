import { truncateUal } from "@/components/ka-catalog/ka-table-meta";

export { truncateUal };

/** Shared column headers so the skeleton and live table stay aligned. */
export const RATE_KA_TABLE_HEADERS = [
  "Title",
  "KA UAL",
  "Status",
  "Action",
] as const;
